
CONFIG_DIR="$HOME/dotfiles/home"

if [ -d "$CONFIG_DIR" ]; then
	echo "rebuilding home manager config"
	home-manager switch --flake $CONFIG_DIR#jaxson
	echo "rebuild finished with exit code $?"
else
	echo "Couldn't find config at $CONFIG_DIR"
fi
